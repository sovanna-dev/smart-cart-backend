# Application Name
spring.application.name=backend

# Server Port
server.port=8080

# ---- PostgreSQL Database Connection (Session Pooler for IPv4) ----
spring.datasource.url=jdbc:postgresql://aws-1-ap-southeast-1.pooler.supabase.com:5432/postgres
spring.datasource.username=postgres.udpdmkfevpjqnxnvstmu
spring.datasource.password=smartcart%123
spring.datasource.driver-class-name=org.postgresql.Driver

# ---- Limit HikariCP Connections ----
spring.datasource.hikari.maximum-pool-size=3
spring.datasource.hikari.minimum-idle=1

# ---- JPA / Hibernate Settings ----
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

